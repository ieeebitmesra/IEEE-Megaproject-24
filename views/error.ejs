<%- layout("/layouts/boilerplate.ejs") %>

<div class="row mt-3">
  <div class="alert alert-danger col-6 offset-3" role="alert">
    <p class="alert-heading"><%= message %></p>
  </div>
</div>

<!-- We generally prefer to show only the message to the user, rather than indicating where the error occurred -->
