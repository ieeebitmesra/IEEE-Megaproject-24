class MusicTrack {
  final String imagePath;
  final String trackTitle;
  final String url; // URL of the music file

  MusicTrack(
      {required this.imagePath, required this.trackTitle, required this.url});
}
